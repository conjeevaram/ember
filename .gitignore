# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs
.venv/
venv/
env/

# MuJoCo / runtime artifacts
MUJOCO_LOG.TXT
*.log
mujoco_videos/

# Training setup + outputs (live on the GPU box, not in the repo)
train_5090/
train_5070ti/
*.mp4
*.npz
*.pkl

# Generated procedural scenes (reproducible via scripts/build_scenes.py --random)
scenes/

# Secrets / local env (API keys — never commit)
.env

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local agent/session transcripts (kept out of version control)
claude_chat.txt
MISSION_PIPELINE_PLAN.md

# Scratch reference screenshots (pose references, not source)
image.png
image copy.png

pipeline.html
recordings/